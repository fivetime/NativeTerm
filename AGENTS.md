# AI 开发指南

本文件适用于整个仓库。修改前阅读相关代码和目录内更具体的 `AGENTS.md`；用户的明确指令优先。默认使用中文沟通，代码标识符及代码注释遵循所在模块的现有风格。

## 开源生态优先：先查找，再决定是否自研

**实现非平凡的通用能力前，必须先检查已有实现并调研 Rust 开源生态；存在合适的库时优先采用，只有没有合适方案时才自行实现。** 不要为了避免增加依赖而默认重复造轮子。

按以下顺序执行：

1. 查找仓库现有模块、`Cargo.toml`、`Cargo.lock` 及 Rust 标准库，优先复用已经具备的能力。
2. 对仓库和标准库未覆盖的通用能力，查找 crates.io、docs.rs、项目官方仓库和官方文档，确认当前版本、API 与维护情况。解析、协议、加密、编码、重试、并发、系统接口等尤其应先调研成熟方案。
3. 根据需求选择合适的库，评估功能匹配、维护活跃度、安全记录、许可证与分发要求、跨平台支持、Rust 工具链兼容性、依赖体积、运行开销及 API 稳定性。
4. 优先使用库的公开 API，仅编写必要的项目适配层。利用 feature flags 限制依赖和平台能力；避免引入与已有依赖功能重复的大型框架。
5. 如果候选库均不适用，先简要记录候选方案及具体原因，再实现最小必要范围。合理原因包括缺失关键功能、平台不支持、许可证不兼容、无法接受的安全风险或有证据的性能开销；“自己写更可控”不足以作为理由。

- 调研结论写入任务说明或 PR 描述；影响长期架构的取舍写入相关设计文档。提供所采用库的官方链接及选择理由，无须为每个依赖另建文档。
- 如果网络不可用，明确说明未完成在线核验，使用本地可验证的信息，不编造版本、维护状态或 API。优先推进不依赖该结论的工作。

## Rust 编码规范

- 使用仓库指定的 stable 工具链和现有 edition。采用惯用、清晰的现代 Rust，不无故升级 edition、工具链或全部依赖。
- 模块职责单一，函数保持可读，优先组合与明确的数据流。仅在存在实际复用或边界需求时引入 trait、泛型或宏，避免推测性的抽象。
- 用枚举、新类型和受控构造表达状态及不变量，减少含义不清的布尔参数、字符串状态和魔法数。公共 API 说明用途、错误条件及重要约束。
- 优先借用与明确所有权；避免无依据的 `clone()`、共享可变状态和全局变量。性能优化需基于测量，不以牺牲正确性和可读性换取猜测中的收益。
- 可恢复错误使用 `Result` 和 `?`，在合适的边界补充操作上下文。禁止静默丢弃关键错误；外部输入、I/O 和正常失败路径不使用 `unwrap()` / `expect()`。测试或已证明的不变量可例外，并说明必要的假设。
- `unsafe` 限制在必要的底层边界；每处用 `SAFETY` 注释解释前置条件、指针有效性、生命周期和线程约束中适用的部分。优先使用安全封装与 RAII，正确管理 句柄及 FFI 资源。
- 不在 GUI 线程执行阻塞网络、进程等待或耗时扫描。沿用现有后台工作机制，控制锁的范围，不持锁等待外部 I/O、回调或子进程。
- 对跨进程通信、外部进程和队列考虑超时、退出、取消、资源回收及背压；不以无限重试掩盖故障。
- 通过 `Command` 的参数 API 启动进程；避免拼接 shell 命令。必须经过 shell 或 `wt` 特殊解析时，按目标解析规则处理参数并验证边界输入。
- 面向用户的新增文案接入现有国际化机制。注释解释原因和约束，避免逐句复述代码。

## 数据与集成安全

- 不在日志、错误、命令行示例或测试产物中泄露密码、私钥及其他凭据；复用现有凭据管理机制。
- 修改 SSH 配置时保留无关内容和格式，复用已有的变更检测、备份、原子写入与校验回滚路径。
- 批量会话操作必须尊重会话归属、锁定状态及现有保护规则，避免误操作用户自己的 Terminal 标签页。

## 修改与验证

- 开始前检查 `git status`，保留用户未提交的工作。只修改任务相关文件，不顺带进行大规模格式化、重构或依赖升级。
- 新增依赖通过 Cargo 管理，提交必要的 manifest 和 `Cargo.lock` 变更，不手工编造锁文件内容。
- 使用 `cargo fmt` 格式化变更；检查 diff，避免把现有未格式化代码或用户修改一并改写。
- 对受影响的 crate 执行适当的检查，例如：

  ```powershell
  cargo check -p <crate-name>
  cargo clippy -p <crate-name> --all-targets -- -D warnings
  cargo test -p <crate-name>
  ```

- 触及可移植 crate（`i18n`、`config`、`session`、`sftp`、`os`、`platform`、`app`）时，另跑 `tools\check-portable.ps1`：在本机对 Linux 与 macOS target 做 clippy（含 tests/examples，不链接），`cfg` 门后的死代码和漏掉的 Unix 分支由它抓住。
- 根据范围选择验证：跨 crate、依赖或协议变更应扩大检查，必要时运行 workspace 检查和测试。简单文档变更无需编译。已有无关警告或失败应如实报告，不通过广泛 `allow` 或无关改动掩盖问题。
- 为新增行为、缺陷修复和重要边界补充有价值的测试，优先检查可观察行为；避免机械复刻实现的测试。重点覆盖解析失败、状态转换、连接中断、重复消息及资源清理等相关情形。
- 标记为 `#[ignore]` 的真实 Terminal 集成测试需要特定环境，可能打开窗口或移动鼠标；先阅读测试头部及其环境要求，不默认执行 `--include-ignored`。
- 完成后检查 diff，说明实际改动、验证结果和仍未验证的范围。不得把未运行的测试或文档中的历史验证结果称为本次通过。
