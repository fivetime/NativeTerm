[package]
name = "native-term-sftp"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
encoding_rs = "0.8"
codepage = "0.1"

[target.'cfg(windows)'.dependencies]
native-term-win = { path = "../native-term-win" }

[dev-dependencies]
tempfile = "3"
