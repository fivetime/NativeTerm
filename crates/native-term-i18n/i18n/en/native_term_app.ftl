# NativeTerm's window. Keep product names ("NativeTerm SSH", Windows
# Terminal, SecureCRT, ssh) and file names untranslated.

## Top bar and settings

settings-toggle = Settings
import-securecrt-button = Import from SecureCRT…
language-label = Language
language-system = System default
theme-label = Theme
theme-system = System default
theme-light = Light
theme-dark = Dark
auto-reconnect-setting = Reconnect dropped sessions automatically (never after a failed login)
close-on-exit-setting = Close NativeTerm's tabs when NativeTerm exits
close-on-exit-hint = Off: the tabs stay, and NativeTerm takes them over again at its next start (until then their tab menu isn't available). On: every session's tab is closed, except locked ones; your own tabs are left alone, and a Terminal window with nothing else in it closes too.
dock-pin = Pin
dock-pin-hint = Docked at the { $edge ->
        [top] top
        [left] left
       *[right] right
    } edge: it slides away when the pointer leaves, unless pinned. Drag it away from the edge to undock.
agent-section = SSH keys and ssh-agent
agent-other = SSH_AUTH_SOCK is set: ssh uses that agent.
agent-checking = Checking…
agent-service = ssh-agent service: { $state }
agent-running = running
agent-stopped = stopped
agent-disabled = disabled (Windows' default)
agent-missing = not installed
agent-unavailable = not a service on this system
agent-no-keys = No key pairs in { $dir }.
agent-key-protected = has a passphrase
agent-key-open = no passphrase
agent-key-unknown = can't tell
agent-loaded = The agent holds keys: connects don't ask for passphrases.
agent-empty = The agent holds no keys yet.
agent-unreachable = The agent doesn't answer.
agent-add = Add my keys to the agent…
agent-add-tab = ssh-add
agent-why = A key has a passphrase, so every connect asks for it. ssh-agent can remember it until you sign out. To turn the service on, run this in PowerShell as administrator, then use "Add my keys":
agent-copy = Copy
agent-admin = NativeTerm doesn't change Windows services itself.
storage-conflicts = { $count } files look like sync conflict copies: { $files }. Compare them with the originals and delete the copies; a copy in config.d is read by ssh too, so its hosts are defined twice.
storage-cloud-only = { $count } files are only in the cloud (OneDrive Files On-Demand): { $files }. ssh may stall or fail reading them; right-click the folder → "Always keep on this device".
storage-not-kept = { $count } files are in a cloud-synced folder and may be freed up: { $files }. Right-click the folder → "Always keep on this device".
agent-hint = Your key has a passphrase but ssh-agent isn't running: every connect will ask for it.
agent-hint-show = How to fix
agent-hint-dismiss = Don't show again
settings-terminal = Windows Terminal: { $dir } ({ $kind })
terminal-kind-packaged = Store package
terminal-kind-portable = portable
terminal-kind-unpackaged = unpackaged
settings-hide-terminal-ssh = Hide Windows Terminal's own SSH profiles
settings-hide-terminal-ssh-hint = Windows Terminal 1.25+ makes a profile for every host in ~/.ssh/config. This adds "Windows.Terminal.SSH" to disabledProfileSources in its settings.json (a copy is kept in NativeTerm's backups); unticking removes it again.
settings-terminal-changed = Windows Terminal's settings.json was changed; the previous version is in { $backup }
settings-terminal-change-failed = Windows Terminal's settings.json wasn't changed: { $error }
settings-profile = "NativeTerm SSH" profile: { $status }

## Session tree

tree-heading = Sessions
tree-reload-hint = Reload ~/.ssh
tree-new-folder = Folder
tree-search-hint = Search name, host, user, note…  (Ctrl+F)
tree-search-clear = Clear the search (Esc)
tree-no-match = No host matches "{ $query }"
tree-favorites = Favorites
menu-favorite = Add to Favorites
menu-unfavorite = Remove from Favorites
tree-recent = Recent
tree-all = All sessions
tree-empty = No hosts yet: right-click a folder, or add a folder first.
tree-main-config = ~/.ssh/config
fab-open = NativeTerm: connect, tabs, sessions
fab-close = Close (Esc)
fab-search-hint = Host or user@host[:port]
fab-active = Active session: { $label }
fab-no-active = No NativeTerm session in the current Terminal tab.
fab-show-main = Show NativeTerm
quick-connect = Connect to { $target }
quick-save = Save…
quick-save-hint = Save as a host in ~/.ssh/config
menu-connect-all = Connect All
menu-connect-all-new-window = Connect All in New Window
menu-new-host = New Host…
menu-rename-folder = Rename Folder…
menu-connect-selected = Connect These { $count } Sessions
menu-connect-selected-new-window = Connect These { $count } in a New Window
menu-install-key-selected = Install My Key on These { $count }…
menu-clear-selection = Clear the Selection
menu-new-plink = New Telnet / Serial Session…
plink-new-title = New Telnet / serial session in { $folder }
plink-edit-title = Edit { $name }
plink-name-hint = shown in the tree and on the tab
field-protocol = Protocol
field-serial-line = Port
field-serial-speed = Speed (baud)
field-serial-format = Data bits, parity, stop bits
field-serial-flow = Flow control
field-backspace = Backspace sends
field-backspace-hint = What the Backspace key sends to the far end: Control-H (0x08) or Control-? (0x7F, "delete"). Ctrl+Backspace sends the other one. Older switches and serial gear usually want Control-H; most Unix hosts want Control-?. NativeTerm's own client does this; PuTTY's plink has no say over the keys.
backspace-control-h = Control-H (0x08)
backspace-delete = Control-? (0x7F)
field-charset-hint = What this host's output is in, when it is not UTF-8 (GBK, Big5, a code page number). Nothing converts along the way — ssh passes bytes through — so NativeTerm tells the tab's console what to expect, which is also what makes typing in that character set arrive right.
field-charset = Character set
plink-ports = Ports here
plink-no-ports = No ports found
plink-common = Common
plink-port-needed = required
plink-user-hint = sent as the login name (optional)
plink-on-login-hint = typed once connected
plink-bad-speed = "{ $speed }" is not a speed
plink-note = Runs with PuTTY's plink in a Terminal tab. Kept in the folder's .nt.toml file, which ssh doesn't read.
protocol-raw = Raw TCP
protocol-serial = Serial
parity-none = None
parity-odd = Odd
parity-even = Even
parity-mark = Mark
parity-space = Space
flow-none = None
notice-port-taken = { $line } is already open in "{ $label }"; a serial port can be used by one session at a time.
putty-options = PuTTY options
putty-options-note = Only options that differ from PuTTY's defaults are kept; ntplink gets them on its command line (plink in a temporary PuTTY saved session while it starts).
putty-page-connection = Connection
putty-terminaltype = Terminal type
putty-pingintervalsecs = Keepalive every (seconds, 0 = off)
putty-tcpnodelay = Disable Nagle (TCP_NODELAY)
putty-tcpkeepalives = TCP keepalives
putty-passivetelnet = Passive negotiation
putty-page-line = Echo and line editing
plink-log = Session log
plink-log-type = Log
plink-log-off = Off
plink-log-text = Text (without escape sequences)
plink-log-raw = All output, byte for byte
plink-log-file = File
plink-log-open-folder = Open Folder
plink-log-note = &H is the host or serial line, &Y&M&D the date, &T the time of connecting, &P the port. An existing file is appended to. Needs ntplink (plink writes no session log).
putty-localecho = Local echo
putty-localedit = Local line editing
putty-auto = Automatic
putty-on = On
putty-off = Off
putty-rfcenviron = RFC 1408 environment (instead of BSD)
putty-supduplocation = Location
putty-supdupcharset = Extended ASCII
putty-supdupmoreprocessing = **MORE** processing
putty-supdupscrolling = Terminal scrolling
plink-alias-kept = Alias: { $alias } (kept, so open tabs and references still work)
session-quiet = no data since { $time }
session-quiet-hint = Nothing has arrived from the serial line since then: the device may be off or unplugged, or just idle.
wizard-sync-title = Sessions on several computers
wizard-sync-intro = Put the session folders in a folder your sync client keeps in step; on the other computer, choose the same folder here and its sessions are used as they are.
wizard-sync-move = Move the session folders to { $name }
wizard-sync-adopt = Use the sessions already in { $name }
wizard-sync-inside = The session folders are in { $name } ({ $path }) and follow it to your other computers.
wizard-sync-none = No OneDrive or Dropbox folder found here. Settings → "Move session folders" can put them in any folder a sync tool keeps in step.
folders-adopted = Now using the { $count } session files in { $path } (another computer's). This computer's own in { $old } are kept but no longer read.
menu-connect = Connect
menu-connect-new-window = Connect in New Window
menu-edit = Edit…
menu-move-to = Move to
menu-install-key = Install My Key…
menu-install-key-all = Install My Key on All…
key-title = Install my key
key-none = There is no public key in { $dir } yet.
key-create = Create a key…
key-create-tab = Create a key
key-refresh = Look again
key-which = Public key
key-hosts = On { $count } hosts: { $names }{ $more ->
        [0] {""}
       *[other] {" "}and { $more } more
    }
key-note = Each host gets a tab, where ssh asks for its password one last time. Linux and other Unix hosts and Windows hosts (OpenSSH server) are supported; network devices that manage keys in their own configuration are not.
key-install = { $count ->
        [one] Install
       *[other] Install on { $count } hosts
    }
key-one-password = Same password on all of them: ask it once
key-one-password-hint = One tab adds the key to the hosts one after another; the password is typed once there and kept in memory only. Hosts where it is wrong are listed at the end.
key-batch-tab = Key → { $count } hosts
key-tab = Key → { $label }
menu-folder-options = Folder Options…
options-folder-title = Options for every host in { $label }
options-folder-note = Every host in this folder gets these (each host is tagged, and a "Match tagged" block at the end of the folder file holds them). A value set on a host itself still wins. Grey: what the first host uses now.
folder-options-unsupported = Folder options need OpenSSH 9.4 or newer (ssh -V); this ssh is older.
folder-options-own-tag = These hosts have a Tag of their own and don't get the folder options: { $hosts }
menu-options = Session Options…
options-title = Session options: { $label }
options-connection = Connection
options-authentication = Authentication
options-algorithms = Algorithms
options-host-key = Host key
options-forwarding = Port forwarding
options-environment = Environment
options-default = Default ({ $value })
options-choose = Choose…
options-use-default = Use the default
options-no-names = This ssh doesn't list them.
options-note = Written into the Host { $alias } block of your ssh config, so plain ssh, scp and VS Code use them too. Empty fields keep what ssh uses now (greyed).
options-forward-agent-note = Agent forwarding lets the remote host use your keys while you are connected; turn it on only for hosts you trust.
options-algorithms-note = A list replaces the default; start it with + to add to the default (e.g. +ssh-rsa for old devices), - to remove, ^ to put first.
options-forwarding-note = One forward per line. X11 forwarding needs an X server on this PC.
opt-connect-timeout = Connect timeout (s)
opt-server-alive-interval = Keepalive interval (s)
opt-server-alive-count-max = Keepalive misses
opt-tcp-keep-alive = TCP keepalive
opt-compression = Compression
opt-address-family = IP version
opt-request-tty = Terminal (TTY)
opt-remote-command = Remote command
opt-log-level = Log level
opt-proxy = Proxy
proxy-none = none
proxy-other = Other command (ProxyCommand)
proxy-invalid = The proxy's address isn't right: { $error }
proxy-and-jump = A jump host is set too ({ $jump }): ssh uses whichever comes first, so keep only one
proxy-note = The proxy is written as a ProxyCommand that NativeTerm relays (SOCKS5 and SOCKS4 let the proxy resolve the host name). Plain ssh, scp and VS Code Remote use it too. A proxy that wants a login: fill in the user name; the password is kept in Windows Credential Manager.
proxy-user = User name
proxy-user-hint = if the proxy wants a login
proxy-user-id-hint = SOCKS4 user id (optional, no password)
proxy-password-hint = the proxy's password
proxy-password-none = No password saved for this proxy login yet: connections fail until one is.
proxy-password-saved = The password is saved in Windows Credential Manager; the ssh config has the user name only.
proxy-password-refused = The proxy refused this password, so it isn't sent again (an account could be locked). Save the right one.
proxy-basic-note = An HTTP proxy's password is sent as Basic: encoded, not encrypted, so it can be seen on the network to the proxy.
proxy-windows-note = A proxy that asks for a Windows login (NTLM or Negotiate) gets one without a password here: Windows answers with the account you are signed in with. Fill in a user name and password only if the proxy wants a different account.
opt-preferred-authentications = Methods, in order
opt-pubkey-authentication = Public key
opt-password-authentication = Password
opt-kbd-interactive-authentication = Keyboard-interactive
opt-identities-only = Only the listed keys
opt-forward-agent = Agent forwarding
opt-pubkey-accepted-algorithms = Key signature algorithms
opt-kex-algorithms = Key exchange
opt-ciphers = Ciphers
opt-macs = MACs
opt-host-key-algorithms = Host key algorithms
opt-strict-host-key-checking = Unknown host keys
opt-update-host-keys = Learn new host keys
opt-check-host-ip = Check the IP too
opt-local-forward = Local (-L)
opt-remote-forward = Remote (-R)
opt-dynamic-forward = SOCKS (-D)
opt-exit-on-forward-failure = Fail if a forward fails
opt-gateway-ports = Allow other PCs
opt-forward-x11 = X11 forwarding
opt-forward-x11-trusted = Trusted X11
opt-set-env = Set variables
opt-send-env = Send variables
menu-forget-key = Forget Host Key…
forget-title = Forget host key
forget-question = Remove the saved host keys of { $alias }? ssh will ask to confirm the new key on the next connect.
forget-note = Only do this when the host really changed (reinstalled, new address). A copy is kept as known_hosts.old.
forget-button = Remove
forget-done = Removed the saved host keys of { $names }
forget-none = { $alias } had no saved host key
menu-delete = Delete…
host-via = via { $jump }
host-alias = alias { $alias }

## Open sessions

sessions-heading = Open sessions ({ $count })
view-tabs = All tabs
view-tabs-hint = Every tab in every Windows Terminal window, yours too (Ctrl+T)
tabs-search-hint = Search tab titles…  (Ctrl+T)
tabs-none = No Windows Terminal tabs found.
tabs-no-match = No tab matches.
tabs-window =
    { $count ->
        [one] Window { $number } · 1 tab
       *[other] Window { $number } · { $count } tabs
    }
tabs-view-list = List
tabs-view-pictures = Pictures (each tab as it looked when last selected)
tabs-not-seen = Not seen yet: select this tab once to get its picture
tabs-seen-at = As of { $time }
sessions-clear-finished = Clear finished
sessions-restored-waiting =
    { $count ->
        [one] One restored session is waiting to connect.
       *[other] { $count } restored sessions are waiting to connect.
    }
sessions-connect-all = Connect all
sessions-close-all = Close all
sessions-one-by-one = Or connect them one by one below.
sessions-empty = Double-click a host; Ctrl+click or Shift+click selects several; right-click a host or folder for more.
session-renamed = now called { $name }
session-renamed-hint = The host was renamed after this tab opened. Windows Terminal keeps the tab's title; clones and reopened tabs use the new name.
session-attempt = attempt { $n }
session-auto-reconnect = auto-reconnect { $n }
session-location = window { $window } · tab { $tab }
session-selected = selected
session-split = split
session-current-title = current title: { $title }
sessions-locate = Locate { $count }
sessions-locate-hint = Only a selected tab shows which session is in it. NativeTerm selects the unclaimed tabs one by one until every session is found, then puts the selection back.
session-not-located-hint = tab not found; last seen in window { $window }, tab { $tab }
notice-located = Found them all (looked at { $count } tabs).
notice-still-unlocated = { $count } sessions still not found; their tabs may be in another window or closed.
session-not-located = not located
button-focus = Focus
button-connect = Connect
button-reconnect = Reconnect
button-disconnect = Disconnect
button-break = Break
button-close = Close
button-save = Save
button-cancel = Cancel
button-delete = Delete
button-check-again = Check again

## Session states

state-opening = opening
state-detached = looking for its tab…
state-waiting = restored, not connected
state-connecting = connecting / waiting for login
state-connected = connected
state-login-failed = login failed ({ $code })
state-unreachable = could not connect ({ $code })
state-disconnected = disconnected ({ $code })
state-ended = ended ({ $code })
state-failed = failed: { $reason }
state-gone = tab gone
state-closed = closed

## Notices

lost-banner = { $count } session(s) from the last run have no tab any more.
lost-banner-moved = { $count } session(s) from the last run have no tab any more: the program folder moved, so the tabs Windows Terminal restored started a helper that is no longer there.
lost-reopen = Open them again
lost-ignore = Leave them
notice-lost-sessions =
    { $count ->
        [one] One session from the last run has no tab any more
       *[other] { $count } sessions from the last run have no tab any more
    }
notice-tab-not-found = { $label }: its tab wasn't found (a split tab is found once it's selected)
notice-tab-gone = The tab “{ $title }” isn't there any more
notice-tab-changed = { $label }: the tab changed, not closed
notice-not-linked = { $label }: its tab isn't connected to NativeTerm
notice-terminal-failed = Windows Terminal could not be started: { $error }
notice-tabs-pending = { $count } tabs were not opened: another Terminal window became active
notice-tabs-resent = { $count } tabs didn't appear in Windows Terminal and were asked for again: { $labels }
notice-tabs-missing = { $count } tabs didn't appear in Windows Terminal: { $labels }
notice-pipe-stranger = A program that is not NativeTerm's helper tried to talk to NativeTerm and was refused: { $program }
notice-pipe-stopped = NativeTerm's pipe server stopped: { $error }
notice-old-shim = A shim with protocol { $protocol } connected (expected { $expected }); update it
notice-gave-up = { $label }: gave up reconnecting after { $tries } tries
notice-db-unavailable = { $path }: { $error }; open sessions won't be remembered
notice-no-data-dir = No data directory: { $error }; open sessions won't be remembered
data-lock-unknown = another NativeTerm
notice-data-dir-busy = The data folder is open in { $holder }. Two NativeTerms sharing one folder overwrite each other's settings; sessions and notes are not affected.
notice-settings-unreadable = { $path } can't be read ({ $error }), so it is left as it is; settings changed now are forgotten when NativeTerm closes.
notice-settings-not-written = { $path } can't be written: { $error }
notice-shim-missing = { $path } is missing; tabs can't start
fatal-no-terminal = Windows Terminal is not installed. Install it from the Microsoft Store, or start NativeTerm with --terminal-dir pointing at a portable copy.
notice-terminal-old = This Windows Terminal is { $version }; NativeTerm needs { $oldest } or newer to tell its tabs apart (the --sessionId it opens them with came with { $oldest }). Update Terminal.
notice-terminal-several = { $count } Windows Terminal installs were found; NativeTerm is using { $dir }. Pick another one under Settings if that is not the one you use.
notice-terminal-choice-gone = The Windows Terminal you chose ({ $chosen }) is no longer there; NativeTerm is using { $dir }.
notice-wt-alias-off = The wt.exe command is turned off for this Windows Terminal (Settings → Apps → Advanced app settings → App execution aliases). NativeTerm starts it from the package instead: { $path }.
notice-wt-missing = No wt.exe was found for { $dir }, so tabs can't be opened. Turn its app execution alias back on (Settings → Apps → Advanced app settings), or reinstall Windows Terminal.
notice-we-are-elevated = NativeTerm is running as administrator and Windows Terminal is not, so its tabs can't reach NativeTerm (Windows doesn't let a normal program write to an administrator's pipe) and nothing can be dragged onto this window. Run NativeTerm as yourself instead.
notice-terminal-elevated = Windows Terminal is running as administrator and NativeTerm is not, so Windows won't let NativeTerm read or drive those windows. Start Terminal as yourself, or run both as administrator.
settings-terminal-version = Version: { $version }
settings-terminal-favorites = Favorites in Windows Terminal's own menus
settings-terminal-favorites-hint = Each favorite host gets a profile of its own in the new-tab dropdown and an entry in the command palette, so it can be opened from Windows Terminal without NativeTerm running. Only favorites, and only while this is on.
settings-terminal-favorites-needs-fragment = This needs the "NativeTerm SSH" profile installed as a fragment.
settings-terminal-favorites-changed = Windows Terminal picks the change up on its own.
settings-terminal-pick = Use this Windows Terminal:
settings-terminal-picked = { $dir } is used the next time NativeTerm starts.
settings-terminal-picked-current = { $dir } is the one already running; NativeTerm keeps using it.
notice-no-pipe = NativeTerm can't serve its pipe: { $error }
notice-no-terminal-backend = No terminal can be driven on this system yet: the list and the settings work, tabs cannot be opened.
notice-wezterm-missing = WezTerm was asked for, but no wezterm program was found (on PATH, or in the folder given).
notice-tab-menu-unavailable = NativeTerm's tab menu isn't available: { $error }
notice-agent-forwarding = { $count } of the { $total } hosts just opened forward your ssh-agent ({ $names }…): while connected, anyone with root there can use your keys. Turn it off in Session Options → Authentication where it isn't needed.
notice-move-failed = Moving { $alias } failed: { $error }
error-host-gone = { $alias } is gone (changed outside NativeTerm?)
fatal-title = NativeTerm can't start
fatal-window = NativeTerm's window can't start: { $error }

## "NativeTerm SSH" profile

profile-updated = Updated the "NativeTerm SSH" profile: { $old } moved to { $new }
profile-update-failed = Could not update the "NativeTerm SSH" profile: { $error }
profile-installed = installed (fragment)
profile-in-settings = defined in this Terminal's settings.json
profile-outdated = points at { $path }
profile-disabled = turned off on Terminal's Extensions page
profile-missing = not installed
profile-banner-disabled = The "NativeTerm SSH" profile is turned off in Windows Terminal (Settings → Extensions → NativeTerm).
profile-banner-missing = Windows Terminal doesn't have the "NativeTerm SSH" profile yet; tabs can't open.
profile-install = Install profile
profile-install-hint = Writes { $path } (read by every Windows Terminal of this user)
profile-install-update = Install / update
profile-remove = Remove
profile-install-failed = Installing the profile failed: { $error }
profile-no-terminal = no terminal to install the profile into on this system
profile-driven-by = Sessions open in { $terminal }, which needs no profile.
terminal-none = no terminal
terminal-found = found
profile-remove-failed = Removing the profile failed: { $error }
profile-no-localappdata = LOCALAPPDATA is not set

## Host and folder dialogs

host-new-title = New host in { $folder }
host-edit-title = Edit { $alias }
host-bad-port = port "{ $port }" is not a number from 1 to 65535
field-name = Name
field-name-hint = shown in the tree and on the tab
field-host = Host
field-host-hint = host name or address
field-user = User
field-user-hint = (ssh default)
field-port = Port
field-jump = Jump host
field-jump-hint = e.g. bastion or user@bastion:22
field-keys = Keys
field-keys-hint = one IdentityFile per line, e.g. ~/.ssh/id_ed25519
notice-notes = Notes: { $error }
field-tags = Tags
field-tags-hint = Words to find this host by, separated by commas. They are searched along with the name and the note, and they travel with the notes (notes.toml), not with the ssh config.
field-tags-hint-short = prod, ceph
field-long-note = Notes
field-long-note-hint = As many lines as you like. Kept in NativeTerm's own data by the host's id, so renaming the host keeps it, and exported to notes.toml so it syncs. The one-line note above is the one that lives in the ssh config.
field-long-note-hint-short = anything worth remembering about this host
host-tags = Tags: { $tags }
field-note = Note
field-note-hint = one line
field-pre-connect = Before connecting
field-pre-connect-hint = A command run on this computer before every attempt (a VPN, a tunnel, mounting a drive). It runs in the tab and the session waits for it; a failure is reported and the connection goes ahead, unless the command is written with ! in front.
field-on-login = After login
field-on-login-hint = typed after every login, e.g. sudo -i
host-alias-kept = ssh alias: { $alias } (kept, so tabs and scripts keep working)
folder-new-title = New folder
folder-rename-title = Rename { $name }
folder-name-hint = folder name
delete-title = Delete host
delete-question = Delete "{ $label }" ({ $alias }) from the ssh config?
delete-backup-note = A backup of the file is kept in the data directory.

## SecureCRT import

import-putty-button = Import from PuTTY…
import-title-putty = Import from PuTTY
import-putty-from = PuTTY saved sessions
summary-ppk = PuTTY keys (.ppk) are not used by OpenSSH: { $count } sessions; convert them with PuTTYgen (Conversions → Export OpenSSH key) and add the key in the host dialog
summary-putty-host-keys-skipped = PuTTY host keys that can't be used by OpenSSH (DSA, Ed448, unreadable): { $count }; ssh asks once for those hosts
import-title = Import from SecureCRT
import-folder-label = SecureCRT config folder
import-into = Into: { $path }  (every changed file is backed up first)
import-not-found = SecureCRT's config folder wasn't found; enter it above.
import-scanning = Reading the configuration… (a folder on OneDrive may have to be downloaded first)
import-preview = Preview
import-run =
    { $count ->
        [one] Import 1 host
       *[other] Import { $count } hosts
    }
import-checking = Each folder is checked with ssh -G after writing.
import-done = Imported { $hosts } hosts into { $folders } folders.
import-folder-failed = Folder { $folder } was not written: { $error }
import-failed = Import failed: { $error }
summary-found = { $sessions } sessions found in { $folders } folders; { $hosts } will be imported into { $new } new and { $existing } existing folders
summary-skipped = Skipped, { $reason }: { $count }
skip-already = already imported
skip-protocol = { $protocol }: not a terminal session NativeTerm opens
skip-no-hostname = no host name
summary-duplicates = Same host, port and user in several sessions: { $count } groups (all imported; review them)
summary-firewall = Firewall/proxy “{ $name }” isn't imported: { $count } sessions connect directly
summary-unresolved-jumps = Jump session not found or not imported: { $count } sessions connect directly
summary-logon-actions = Logon actions/scripts aren't imported: { $count } sessions
summary-saved-passwords = Saved passwords aren't imported ({ $count } sessions); use keys or ssh-agent
summary-encodings = Non-UTF-8 character sets (OpenSSH sessions are UTF-8): { $count } sessions
summary-bad-forwards = Unreadable port forwards left out: { $count } sessions
summary-also = Also imported: { $items }
summary-forwards = { $count } port forwards
summary-keys = { $count } key files (must be in OpenSSH format)
summary-descriptions = { $count } multi-line descriptions joined into one line
summary-unreadable = Unreadable session files: { $count }
summary-not-utf8 = Session files that aren't UTF-8 (read with replacement characters): { $count }
summary-host-keys = Host keys: { $count } (added to known_hosts unless already there)
summary-host-keys-unknown = Host key files that weren't understood (left out): { $count }
import-keys-added = Added { $count } host keys to known_hosts.
import-keys-failed = known_hosts was not changed: { $error }
summary-not-yet = Not imported yet: button bar and Command Manager commands

## Tab menu (on NativeTerm's tabs in Windows Terminal)

tabmenu-header-mixed = { $label } · this tab has other panes too
tabmenu-header-titled = { $label } · titled “{ $title }”
tabmenu-connect = Connect
tabmenu-reconnect = Reconnect
tabmenu-disconnect = Disconnect
tabmenu-clone = Clone Session
tabmenu-lock = Lock
tabmenu-unlock = Unlock
session-lock = Lock
session-unlock = Unlock
session-locked = Locked
session-lock-hint = A locked session is left out of "close disconnected", "close others", "close to the right" and group sends, and must be unlocked before closing it here.
session-kept-on-server = · kept on the server (tmux)
session-kept-on-server-hint = This session's shell runs in tmux on the server: after a dropped connection, a closed tab or a restart, reconnecting lands back in the same shell with its programs still running. Typing exit in the shell ends it.
send-locked = locked: not ticked by "All"
send-through-tmux = { $state }: through tmux on the server
send-through-tmux-hint = This host's sessions are kept in tmux on the server ({ $name }). While its tab isn't logged in, the command goes there with tmux send-keys over a connection of its own (key or ssh-agent login needed).
send-tmux-pending = Sending through tmux ({ $count })…
send-tmux-sent = { $label }: sent through tmux on the server
send-tmux-none = { $label }: its tmux session isn't on the server (ended, or never started); not sent
send-tmux-failed = { $label }: sending through tmux failed: { $error }
tabmenu-clear = Clear Screen and Scrollback
tabmenu-rename = Rename…
tabmenu-break = Send Break
field-persistent = Keep on the server
field-persistent-hint = Run the shell inside tmux (or screen) on the server: it keeps running when the connection drops, the laptop sleeps or the tab is closed, and reconnecting lands back in it. Scrolling then goes through tmux. Hosts without tmux get a plain shell. "Recorded on the server": tmux also appends everything the session shows to ~/.nativeterm/logs/<session>.log there, while you are disconnected too (the first pane of the session).
persistent-folder = As the folder ({ $value })
persistent-off = Off
persistent-tmux-log = tmux, recorded on the server
host-persistent-log = recorded in ~/.nativeterm/logs
menu-folder-persistent = Keep sessions on the server
host-persistent = Kept on the server ({ $program })
notice-password-refused = { $label }: the server refused the saved password. It is not used again until you save a new one (Edit the host; for a credential set, Credential Sets…).
password-title = Saved password (optional)
password-none = No password saved: ssh asks in the tab.
password-saved = Saved in Windows Credential Manager ({ $target }); given to ssh's own password prompt for this account, once per connect.
password-refused = The server refused the saved password, so it isn't used. Save the current one, or remove it.
password-title-set = Password of the credential set "{ $set }"
password-none-set = This credential set has no password yet: type one below and save it to create it.
password-refused-set = A server refused this credential set's password, so it isn't used. Save the current password here and every host that uses the set uses it again.
password-hint-set = the set's (shared) password
password-saved-set = Saved. Several hosts share this password: changing it here changes it for every host that uses the set.
field-credential = Credential set
field-credential-hint = One saved password shared by several hosts (NativeTermCredential). The user still comes from the ssh config.
credential-folder = As the folder ({ $value })
credential-none = None (the host's own password)
credential-new = New…
credential-folder-none = None
menu-folder-credential = Credential Set
cred-sets-button = Credential Sets…
cred-sets-title = Credential Sets
cred-sets-intro = A credential set is one saved password shared by several hosts (kept in Windows Credential Manager). Pick a set in the host dialog or the folder menu; the user still comes from the ssh config.
cred-sets-none = No credential sets yet.
cred-sets-used = used by { $count } host(s)
cred-sets-refused = refused by the server, not used
cred-sets-new-password = new password
cred-sets-saved = The password of "{ $name }" was updated.
cred-sets-remove-confirm = Really remove ({ $count } host(s) use it)
cred-sets-removed = "{ $name }" was removed. Its hosts ask for the password when they connect.
cred-sets-missing = The ssh config uses these credential sets, but Credential Manager has none of them (their hosts ask for the password): { $names }
cred-sets-add = New credential set
cred-sets-name-hint = name
cred-sets-password-hint = password
cred-sets-add-button = Add
cred-sets-bad-name = A name can't hold spaces, quotes or / \ : * ?, can't be "none", and has at most 64 characters.
cred-sets-exists = There is already a credential set named "{ $name }".
cred-sets-added = Credential set "{ $name }" added.
password-hint = password of { $account }
password-save = Save Password
password-remove = Remove
password-stored = Saved.
password-removed = Removed.
password-after-save = A password can be saved once the host is saved (Edit).
password-warning = Any program running as you can read saved passwords without asking. Keys with ssh-agent are safer (Install My Key).
send-line-to = To
send-line-active = { $label }
send-line-no-active = no active session
send-line-all = all logged-in ({ $count })
send-line-left-out = { $count } left out
send-line-left-out-hint = Locked sessions and folders marked "No group send" aren't sent to "all". Use Send… on a session to reach one of them.
send-line-hint = command, Enter sends (↑ ↓ earlier ones)
send-line-confirm = Send to { $count } sessions? Enter again, or Esc
send-line-send = Send
send-line-nobody = No logged-in session to send to.
send-line-sent = sent to { $count }
send-line-skipped = not logged in: { $names }
send-line-failed = failed: { $names }
menu-folder-no-group-send = No group send
menu-folder-no-group-send-hint = Leave this folder's sessions out when sending to several sessions (e.g. production); a single session can still be sent to
field-tab-color = Tab color
field-tab-color-hint = The color of this host's tabs in Windows Terminal, e.g. red for production, so they can't be mistaken for others
field-color-scheme = Color scheme
look-folder = As the folder ({ $value })
look-none = None
look-custom = Custom…
look-terminal-default = Terminal's default
color-red = Red
color-orange = Orange
color-yellow = Yellow
color-green = Green
color-blue = Blue
color-purple = Purple
menu-folder-tab-color = Tab Color
menu-folder-color-scheme = Color Scheme
host-look = Tab { $color } · scheme { $scheme }
import-run-commands = Import { $count } Hosts and { $commands } Commands
buttons-found = Button bars: { $commands } commands from { $bars } bars ({ $file }), into the command library, grouped by bar
buttons-skipped = { $count } buttons not imported
buttons-why-function = { $function } has no counterpart (only "Send String" buttons become commands)
buttons-why-substitution = uses { $code }, which SecureCRT fills in or does itself (pause, user name, clipboard, local address…)
buttons-why-empty = nothing to send
buttons-added = Command library: { $added } added, { $present } there already, { $renamed } renamed because the name was taken
buttons-unreadable = The button bar file can't be read: { $error }
buttons-library-failed = The command library wasn't changed: { $error }
menu-server-sessions = Sessions on the Server…
menu-server-sessions-hint = NativeTerm's tmux / screen sessions on this host: reopen one in a tab, or end it
server-sessions-title = Sessions on { $label }
server-sessions-intro = Kept on the server by tmux or screen (Keep on the server). A closed tab's session is still here: open it again, or end it.
server-sessions-asking = Asking the server…
server-sessions-batch = Asked with ssh in the background, which can't type a password: this needs a key or ssh-agent.
server-sessions-none = No NativeTerm sessions on this server.
server-sessions-in-tab = in tab { $label }
server-sessions-attached = attached elsewhere
server-sessions-detached = detached
server-sessions-show = Show Tab
server-sessions-open = Open
server-sessions-end = End…
server-sessions-end-hint = Stops the shell and everything running in it
server-sessions-end-now = End Now
server-sessions-keep = Keep
server-sessions-refresh = Refresh
server-log-view = Log
server-log-ended = ended, log kept
server-log-title = Log of { $name } (the end of it)
server-log-note = On the server: ~/{ $file }. Escape sequences are left out here; "Save a Copy" keeps everything.
server-log-copy = Save a Copy
server-log-copy-hint = The whole log, as it is, into NativeTerm's data folder (logs\server)
server-log-delete = Delete on the Server…
server-log-delete-now = Delete Now
server-log-copying = Copying the log of { $name }…
server-log-copied = Saved: { $file }
server-log-failed = The log couldn't be copied: { $error }
server-sessions-opened = Opening { $name } in a new tab.
server-sessions-opened-several = Opening { $count } sessions in new tabs.
server-sessions-open-all = Open All ({ $count })
server-sessions-open-all-hint = Opens every session that is attached nowhere and in no tab (after a restart, say) in new tabs
server-sessions-no-hosts = No host in this folder is kept on the server.
menu-folder-server-sessions-hint = NativeTerm's tmux / screen sessions on the hosts of this folder, in one list
menu-folder-server-sessions-none = No host in this folder is kept on the server
notice-dialog-open = Finish the open dialog in NativeTerm first.
notice-not-saved = { $alias } isn't a saved host; open it from the tree to edit it.
close-mixed-title = Close tabs with other panes?
close-mixed-what = { $count } sessions would be closed; { $mixed } of them are in a tab that holds other panes.
close-mixed-hint = Closing such a tab closes the other panes in it too — they may be your own shells.
close-mixed-close = Close { $count }
tabmenu-close = Close
tabmenu-close-mixed = Close This Session (keeps the other panes)
tabmenu-close-others = Close Other NativeTerm Tabs
tabmenu-close-disconnected = Close Disconnected Tabs
tabmenu-close-right = Close Tabs to the Right

## Sending commands

send-title = Send command
send-command-label = Command (one per line)
send-enter = Press Enter after the last line
send-saved = Saved commands
send-saved-pick = (choose)
send-saved-none = No saved commands yet.
send-save-as = Save as
send-save = Save
send-delete = Delete this saved command
send-library-error = { $path } can't be read ({ $error }); it isn't changed.
send-targets = Sessions
send-all = All reachable
send-none = None
send-no-sessions = No open sessions.
send-not-logged-in = not logged in ({ $state })
send-button =
    { $count ->
        [one] Send
       *[other] Send to { $count } sessions
    }
send-confirm = Really type this into { $count } sessions?
send-back = Back
send-result-sent = Sent to { $count }: { $labels }
send-result-skipped = Not sent (not logged in): { $labels }
send-result-failed = Not sent (NativeTerm lost the tab): { $labels }
session-send = Send…
session-break-hint = Send a Break: a serial line held low (e.g. into a router's ROM monitor), or Telnet's Break command
sessions-send-many = Send to several…
tabmenu-send = Send Command…

folders-current = Session folders: { $path }
folders-change = Move session folders…
folders-move = Move
folders-move-note = The folder files (*.conf) are copied there and the Include line in ~/.ssh/config is changed; ssh must accept it and see the same hosts, or nothing changes. The old folder stays as it is. A synced folder (OneDrive) shares your sessions between PCs.
folders-moved = Moved { $count } folder files to { $path }; ssh reads them from there now. { $old } is left as it was.
folders-move-failed = The session folders weren't moved: { $error }
folders-missing = The session folders { $path } aren't there (a sync folder not available?); new folders go there when it is back.
data-dir-current = NativeTerm data: { $path } (from { $source })
data-dir-fixed = Chosen with --data-dir or NATIVETERM_DATA_DIR; change it there.
data-dir-change = Change data folder…
data-dir-move = Copy and use from next start
data-dir-move-note = The folder must be new or empty. Everything is copied (state.db as a consistent snapshot); the old folder stays until you delete it.
data-dir-moved = Copied { $count } files to { $path }. Restart NativeTerm to use it; { $old } is kept until you delete it.
data-dir-move-failed = The data folder wasn't changed: { $error }
wizard-open = First-run guide…
wizard-title = Welcome to NativeTerm
wizard-step = Step { $n } of { $total }: { $title }
wizard-check = Check this PC
wizard-import = Bring your sessions
wizard-keys = Log in with a key
wizard-data = Where things are kept
wizard-check-intro = NativeTerm opens sessions as { $terminal } tabs running OpenSSH. Here is what it found:
wizard-ssh-checking = Asking ssh for its version…
wizard-ssh-ok = OpenSSH: { $version }
wizard-ssh-missing = No usable ssh.exe ({ $error }). Install the "OpenSSH Client" optional feature in Windows Settings → System → Optional features.
wizard-ssh-missing-unix = No usable ssh ({ $error }). Install the OpenSSH client (openssh-client).
wizard-terminal = { $name }: { $terminal }
wizard-profile = "NativeTerm SSH" profile: { $status }
wizard-agent-running = ssh-agent is running: key passphrases are asked once.
wizard-agent-needed = A key has a passphrase but ssh-agent isn't running: every connection will ask for it.
wizard-agent-off = ssh-agent isn't running (only needed for keys with a passphrase).
wizard-import-intro = Your sessions live in ~/.ssh/config and config.d (now { $hosts } hosts). Import existing ones:
wizard-securecrt-found = found: { $path }
wizard-securecrt-not-found = SecureCRT's configuration wasn't found; you can pick the folder in the dialog.
wizard-putty-none = PuTTY has no saved sessions on this PC.
wizard-import-later = Both imports are also in the toolbar, any time. Passwords are never imported.
wizard-keys-intro = With a key, ssh logs in without asking for the host's password.
wizard-keys-found = Public keys found: { $count }
wizard-install-keys = { $count ->
        [one] Install my key on this host…
       *[other] Install my key on { $count } hosts…
    }
wizard-data-intro = Sessions are plain ssh config, shared with ssh, scp and VS Code. NativeTerm's own data (recent hosts, open tabs, backups, command library) is kept separately:
wizard-sessions-dir = Sessions
wizard-data-dir = NativeTerm data
wizard-data-move = Move NativeTerm data to
wizard-open-folder = Open
wizard-sync-note = Private keys are never synced (a new key per computer is safer: "Install my key"). Syncing through other storage (S3, Google Drive, …) with rclone comes later.
wizard-back = Back
wizard-next = Next
wizard-skip = Skip the guide
wizard-finish = Finish
menu-files = Files (SFTP)…
menu-files-hint = Browse the host's files, upload, download and edit them over SFTP (nothing to install on the server)
files-title = Files – { $label }
files-connecting = Connecting to { $host }…
files-listing = Reading the folder…
files-batch = SFTP connects with ssh in the background, with the same config, keys and ssh-agent as the terminal; a saved password is filled in, anything else is asked here.
files-reconnect = Connect Again
files-up = Up (Backspace)
files-refresh = Refresh (F5)
files-upload = Upload…
files-new-folder = New Folder
files-new-folder-hint = Folder name
files-encoding-auto = Auto
files-encoding-hint = How the server's file names are encoded: Auto is UTF-8, else the system's code page. Old servers may use GBK, Big5, Shift_JIS, …
files-col-name = Name
files-col-size = Size
files-col-modified = Modified
files-col-mode = Permissions
files-download = Download
files-download-to = Download To…
files-edit = Edit
files-rename = Rename (F2)
files-copy-path = Copy Path
files-delete = Delete
files-delete-confirm = Delete { $what } on the server?
files-delete-folders = Folders are deleted with everything in them.
files-delete-now = Delete Now
files-close-title = Close the Files Window?
files-close-running = { $count } transfer(s) still running will stop; transferring the same files again goes on where they stopped.
files-close-edits = { $count } edited file(s) will no longer be uploaded when saved.
files-close-now = Close Anyway
files-drop-here = Drop to upload to { $path }
files-bad-name = "{ $name }" can't be used as a name here (or can't be written in the server's file name encoding).
files-pick-upload = Files to upload
files-pick-folder = Download to folder
files-job-upload = Upload { $what }
files-job-download = Download { $what }
files-job-delete = Delete { $what }
files-job-cancelled = { $what }: cancelled
files-job-left = { $time } left
files-job-pause = Pause
files-job-resume = Resume
files-job-resume-hint = Go on where it stopped: files already copied are skipped, a partly copied one goes on after its copied part
files-job-remove = Remove from the queue
files-job-paused = { $what }: paused
files-job-paused-short = paused
files-job-not-connected = The session isn't connected: Reconnect first, then resume the transfer.
files-open-folder = Open Folder
files-and-more = { $first } and { $count } more
files-edit-opening = downloading…
files-edit-watching = opened; saving uploads it again
files-edit-uploading = uploading…
files-edit-uploaded = uploaded at { $time }
files-edit-failed = failed: { $error }
files-edit-conflict = changed on the server since it was opened: not uploaded
files-edit-overwrite = Overwrite
files-edit-stop = Stop
files-edit-disconnected = disconnected: no longer uploaded
button-create = Create
files-download-hint = Into your Downloads folder (right-click: Download To…)
files-no-downloads = The Downloads folder wasn't found: use Download To… in the right-click menu.
files-edit-hint = Open it with its program; saving uploads it again (Enter, or double-click)
files-question-title = { $host } asks
button-ok = OK
files-window-title = NativeTerm Files
files-local-tab = Local ({ $computer })
files-computer = This PC
files-upload-to-remote = Upload
files-upload-hint = The selected files and folders to the server's folder on the right (or drag them there)
files-download-to-local = Download
files-download-hint = The selected files and folders to the local folder on the left (or drag them there)
files-open = Open
files-recycle-confirm = Move { $what } to the Recycle Bin?
files-recycle-now = Move to Recycle Bin
files-close-tab = Close this session
files-no-tabs = Open a session with Files (SFTP)… in a host's menu or a terminal tab's menu.
files-local-no-folder = Choose a local folder on the left first.
files-drop-remote-only = Drop files on the server's side (right) to upload them.
files-queue = Transfers
files-queue-clear = Clear Finished
files-sync = Synchronize…
files-sync-hint = Compare the local folder on the left with the server's folder on the right (and all folders below them), list what differs, then choose how to synchronize
files-sync-title = Synchronize — { $host }
files-sync-local = Local
files-sync-remote = Server
files-sync-both = Both ways
files-sync-both-hint = What one side lacks is copied to it; of two different files the newer one replaces the older
files-sync-upload = Local is the source
files-sync-upload-hint = Upload new and different local files to the server
files-sync-download = Server is the source
files-sync-download-hint = Download new and different files from the server
files-sync-delete-extra = Delete extra files on the target
files-sync-delete-extra-hint = Files and folders on the target side that the source doesn't have are deleted (on the server for good, locally to the Recycle Bin)
files-sync-show-all = Show files that are the same
files-sync-comparing = Comparing… { $count } looked at
files-sync-cancelled = Comparing cancelled
files-sync-rescan = Compare Again
files-sync-alike = Both sides are alike: nothing to synchronize.
files-sync-summary-up = Upload { $count } ({ $size })
files-sync-summary-down = Download { $count } ({ $size })
files-sync-summary-delete = Delete { $count }
files-sync-summary-conflicts = { $count } can't be decided, skipped
files-sync-conflict-hint = A file on one side and a folder of that name on the other; or the same time with different sizes; or a name the server's encoding can't write
files-sync-start = Synchronize
files-sync-confirm-delete = { $count } will be deleted (on the server for good, locally to the Recycle Bin).
files-sync-start-delete = Delete and Synchronize
files-sync-folder = folder
files-sync-size-folders = { $size } and { $count } folder(s)
files-sync-folders = { $count } folder(s)
files-sync-col-act = Action
files-sync-col-path = Path
files-sync-act-upload = Upload
files-sync-act-download = Download
files-sync-act-delete-local = Delete local
files-sync-act-delete-remote = Delete on server
files-sync-act-same = Same
files-sync-act-skip = Kept
files-sync-act-conflict = Can't decide
files-job-sync-upload = Synchronize: upload { $count }
files-job-sync-download = Synchronize: download { $count }
files-queue-empty = No transfers. Select files and click Upload or Download, or drag them to the other side (or from Explorer onto the server's side).
files-queue-running = { $count } running
files-queue-done = { $count } done
files-queue-failed = { $count } failed
files-queue-paused = { $count } paused
files-queue-pause-all = Pause All
files-queue-resume-all = Resume All
files-status-items = { $folders ->
    [one] 1 folder
   *[other] { $folders } folders
}, { $files ->
    [one] 1 file
   *[other] { $files } files
}
files-status-selected = { $count } selected ({ $size })
files-status-connected = Connected
files-status-connecting = Connecting…
files-status-failed = Not connected
files-log-connecting = Connecting to { $host }…
files-log-connected = Connected; folder { $path }
files-log-done = { $what }: done
tabmenu-files = Files (SFTP)
notice-files-ssh-only = { $label } isn't an SSH session: it has no files to show (SFTP needs SSH).
keys-title = Keyboard Shortcuts
keys-intro = "In the window" works only in NativeTerm's window; "Global" also works while another program, such as Windows Terminal, is in front (none is on by default). "The active session" is the selected tab of the Terminal window last in front. Click a button, then press the new combination; Esc cancels.
keys-command = Command
keys-local = In the window
keys-global = Global
keys-none = (none)
keys-press = Press a combination… (Esc cancels)
keys-clear = Clear
keys-defaults = Restore Defaults
keys-taken = Same as "{ $command }"
keys-not-global = A global shortcut needs Ctrl or Alt and can't be one Windows keeps (Alt+F4, Alt+Tab, Alt+Space…); not on
keys-needs-modifier = Needs Ctrl or Alt (except F1–F24), or it would fire while typing in a text field; not on
keys-terminal = Windows Terminal uses it for { $action }: once on, pressing it in the Terminal runs NativeTerm's command instead
keys-held = Another program holds it; not on ({ $error })
keys-no-active = No active session: switch to a NativeTerm tab in Windows Terminal first.
keys-cmd-show = Show NativeTerm and Search Hosts
keys-cmd-search = Search Hosts
keys-cmd-tabs = All Tabs
keys-cmd-send-active = Send Command to the Active Session
keys-cmd-send-several = Send to Several Sessions
keys-cmd-files-active = Files (SFTP) of the Active Session
drop-title = Dropped files
drop-what = { $count } file(s) dropped into "{ $label }". What should happen with them?
drop-folder = folder
drop-size = { $size } in all
drop-where = They go to the session's current folder (a tmux session) or the one last used; the files window can change that.
drop-remember = Always do this, do not ask again (the setting can be changed back)
drop-upload = Upload to the server
drop-text = Type the names into the terminal
drop-no-sftp = This session is not SSH: nothing can be uploaded.
notice-drop-not-sent = The text did not reach the session.
drop-ask-setting = Ask about dropped files
drop-ask-hint = With it off, dropped files are dealt with as they were last time (uploaded, or typed into the terminal). Turning it off here does nothing: to stop the question, tick "always do this" when files are dropped.
files-at-once = At once
files-at-once-hint = How many files each connection copies at the same time; the rest wait their turn. More is faster for many small files; fewer is kinder to a slow link or a server that limits them.
drop-size-folders = Folders go with everything in them ({ $size } and what they hold)
tabs-text-preview = text preview
tabs-hover-setting = Show a card when the mouse rests on a tab
tabs-hover-hint = After about half a second, the tab's picture and the session's state appear under it. Terminal's own title tooltip still shows.
tabs-switcher-setting = Ctrl+Tab shows a grid of tab pictures
tabs-switcher-hint = In a Terminal window that holds NativeTerm tabs, Ctrl+Tab shows every tab of that window as a picture instead of Terminal's own list. Hold Ctrl and press Tab (or the arrow keys) to move, let Ctrl go to switch, Esc to leave things as they are. Terminal's own Ctrl+Tab comes back when this is off.
config-paths-repaired = The program moved: { $count } proxy command(s) in the ssh config now point at this copy (they named { $old })

# Clean up (before the program is deleted)
cleanup-button = Clean up…
cleanup-title = Clean up
cleanup-intro = Before deleting NativeTerm, let it take back the Windows Terminal profile it installed. Everything else below stays where it is; it is listed so you know what is there.
cleanup-fragment-title = What only NativeTerm can remove
cleanup-fragment-there = The "NativeTerm SSH" profile is installed in Windows Terminal. Remove it before deleting the program, or Terminal keeps a profile that starts something that isn't there.
cleanup-fragment-gone = The "NativeTerm SSH" profile is not installed.
cleanup-in-settings = This Windows Terminal also has a "NativeTerm SSH" profile written into its own settings.json. NativeTerm did not put it there and leaves it alone; remove it yourself if you want it gone.
cleanup-remove-fragment = Remove the profile
cleanup-left-title = What stays behind
cleanup-data = Data folder: { $path }
cleanup-registry = The data folder is also written in the registry (HKCU\Software\NativeTerm\DataDir = { $value }).
cleanup-credentials = { $count } saved password(s) in Windows Credential Manager.
cleanup-credentials-gone = { $count } saved password(s) removed.
cleanup-remove-credentials = Remove them
cleanup-remove-registry = Remove it
cleanup-registry-gone = The registry value is gone.
cleanup-really = Really?
cleanup-ssh = { $count } line(s) NativeTerm wrote in your ssh configuration:
cleanup-ssh-hint = These do no harm: ssh skips the NativeTerm keys, and the Include line is what makes your session folder count. Removing them takes your sessions away from ssh too, so NativeTerm leaves them.
cleanup-ssh-none = NativeTerm has written nothing into your ssh configuration.
cleanup-files = Your sessions themselves are { $count } file(s) in the session folder; they are yours and stay.
cleanup-copy = Copy this list

# short messages in the corner of the window
toast-closed = Closed { $count } { $count ->
        [one] tab
       *[other] tabs
    }
toast-connecting = Connecting { $count } sessions

# the look on top of light and dark
theme-look-label = Look
theme-look-hint = Colours and spacing on top of light and dark. Every NativeTerm window follows it.
theme-look-plain = Standard
theme-look-accent = Windows accent colour
theme-look-dim = Soft
theme-look-compact = Compact
toast-cleared = Cleared { $count } finished { $count ->
        [one] session
       *[other] sessions
    }

# files ssh refuses because someone else may change them
storage-too-open = { $count } file(s) can be changed by someone other than you, so ssh refuses to use them ("Bad owner or permissions"): { $files }
storage-fix-permissions = Fix permissions
storage-fix-hint = Gives full control to you, Administrators and SYSTEM only, and takes inherited entries off — what NativeTerm and ssh-keygen write themselves.
storage-fixed = Put the permissions back on { $count } file(s).
storage-fix-failed = { $path } was not changed: { $error }

# what this is, and what it doesn't do
about-title = NativeTerm { $version }
about-no-telemetry = Sends nothing anywhere: no telemetry, no update pings, no accounts. Everything it keeps is in the data folder above and in your ssh configuration.

# dragging hosts into another folder
tree-dragging = { $count ->
        [one] Moving 1 host
       *[other] Moving { $count } hosts
    }
tree-drag-hint = Drag a host onto a folder to move it there (several, if several are selected).

# dropping files on NativeTerm's own window
drop-on-session = Drop for { $label }
drop-on-tab = Drop for this tab
