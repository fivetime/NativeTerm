# Lines the shim prints into the terminal tab.

reopening = [NativeTerm] Reopening this restored session in a new tab…
restored = [NativeTerm] { $alias }: restored, not connected yet.
ssh-not-started = [NativeTerm] Could not start ssh ({ $path }): { $error }
login-failed = [NativeTerm] Login failed or cancelled (exit code { $code }).
persistent-session = [NativeTerm] Attaching to { $program } session { $name } on the server (kept after a disconnect).
persistent-log = [NativeTerm] Recorded on the server in ~/{ $file }.
persistent-missing = [NativeTerm] { $program } is not installed on this host: a plain shell, not kept after a disconnect.
persistent-own-command = [NativeTerm] { $alias } has its own RemoteCommand: the session is not kept on the server.
saved-password-refused = [NativeTerm] The saved password was refused. It is not used again until you save a new one (Edit this host).
saved-password-refused-set = [NativeTerm] The password of the credential set "{ $set }" was refused. No host uses it again until you save a new one (NativeTerm → Credential Sets).
unreachable = [NativeTerm] Could not connect to the server (exit code { $code }).
disconnected = [NativeTerm] Disconnected (exit code { $code }).
ended = [NativeTerm] Session ended (exit code { $code }).
reconnect-or-close = [NativeTerm] Press R to reconnect, C to close this tab.
any-key = [NativeTerm] Press any key to close.
key-installing = [NativeTerm] Adding { $path } to { $alias }; ssh may ask for the password.
key-windows-host = { $alias } is a Windows host: adding the key the PowerShell way (ssh may ask for the password once more).
key-installed = [NativeTerm] Done: { $alias } now accepts the key.
key-batch-intro = [NativeTerm] Adding { $path } to { $count } hosts, one after another.
key-batch-password = [NativeTerm] Their password, used for every host and kept in memory only (Enter: each host asks): 
key-batch-each = [NativeTerm] Each host asks for its own password.
key-batch-no-helper = [NativeTerm] The password can't be shared ({ $error }); each host asks for its own.
key-batch-summary = [NativeTerm] Finished: { $installed } added, { $present } had it already, { $failed } failed.
key-batch-failed-hosts = [NativeTerm] Failed: { $hosts } (a different password? install on them one by one).
key-present = [NativeTerm] { $alias } already had the key.
key-failed = [NativeTerm] The key was not added to { $alias } (exit code { $code }). Network devices without a POSIX shell can't take it this way.
key-login-refused = [NativeTerm] { $alias } refused the login (a wrong password?); the key was not added.
key-unreadable = [NativeTerm] { $path } can't be used: { $error }
key-creating = [NativeTerm] Creating a key pair at { $path }. A passphrase protects it (ssh-agent can remember it).
key-created = [NativeTerm] Created. The public key is { $path }.
key-exists = [NativeTerm] { $path } exists already; nothing was changed.
key-create-failed = [NativeTerm] No key was created: { $error }
agent-adding = [NativeTerm] Adding your keys to ssh-agent; type each key's passphrase once.
agent-added = [NativeTerm] Done: until you sign out, ssh won't ask for these passphrases again.
agent-add-failed = [NativeTerm] ssh-add failed: { $error }. Is the ssh-agent service running?
plink-connecting = [NativeTerm] Connecting to { $target } ({ $protocol }) with { $client }…
plink-disconnected = [NativeTerm] Disconnected.
plink-failed = [NativeTerm] Could not connect (exit code { $code }).
plink-gone = [NativeTerm] { $alias } is no longer a saved session.
plink-missing = [NativeTerm] Neither ntplink.exe nor plink.exe was found: put ntplink.exe next to NativeTerm, or install PuTTY.
plink-not-started = [NativeTerm] Could not start { $client } ({ $path }): { $error }
plink-bad-session = [NativeTerm] { $alias } can't be used: { $error }
plink-no-log = [NativeTerm] No session log: plink can't write one (ntplink can).
plink-log-folder = [NativeTerm] The log folder { $folder } couldn't be created: { $error }
plink-no-control = [NativeTerm] Break and Telnet commands are unavailable in this connection: { $error }
plink-load-failed = [NativeTerm] The session's PuTTY options couldn't be passed on: { $error }
port-busy = [NativeTerm] { $line } is in use by another program or session.
port-unavailable = [NativeTerm] { $line } can't be opened: { $error }

## the ProxyCommand helper (--proxy)
proxy-failed = Couldn't reach { $host }:{ $port } through the proxy { $url }: { $error }
proxy-unknown = The proxy { $proxy } wasn't found ({ $error })
proxy-unreachable = Couldn't connect to the proxy { $proxy } ({ $error })
proxy-broken = The connection to the proxy broke ({ $error })
proxy-needs-login = The proxy wants a user name and password, which NativeTerm doesn't support yet
proxy-not-socks = This isn't a { $kind } proxy (its answer doesn't fit): check the proxy type and port
proxy-not-http = This isn't an HTTP proxy (its answer doesn't fit): check the proxy type and port
proxy-refused = The proxy refused the connection: { $reason }
proxy-socks4-v6 = SOCKS4 can't reach IPv6 addresses: use a SOCKS5 or HTTP proxy
proxy-no-answer = The proxy didn't answer: check the proxy type and port
proxy-login-refused = The proxy refused the user name or password (marked, so it isn't tried again by itself: save a new password under Proxy in the session options)
proxy-password-refused = This proxy password was refused before and isn't sent again: save a new one under Proxy in the session options
proxy-no-password = No password is saved for the proxy { $proxy }: save one under Proxy in the session options
proxy-login-too-long = A SOCKS5 user name or password can't be longer than 255 bytes
proxy-schemes = The proxy only takes these logins: { $schemes }; NativeTerm supports Basic only for now
